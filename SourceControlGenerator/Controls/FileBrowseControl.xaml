<UserControl x:Class="LL.SCG.Controls.FileBrowseControl"
			 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
			 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
			 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
			 xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
			 xmlns:local="clr-namespace:LL.SCG.Controls"
			 xmlns:behavior="clr-namespace:LL.SCG.Controls.Behavior"
			 xmlns:conv="clr-namespace:LL.SCG.Converters"
			 mc:Ignorable="d" 
			 d:DesignHeight="300" d:DesignWidth="700"
			 Width="Auto"
			 x:Name="FileBrowser">
	<UserControl.Resources>
		<conv:FileValidationColorConverter x:Key="FileValidationColorConverter" />
	</UserControl.Resources>
	<Grid MaxHeight="20" Width="Auto" HorizontalAlignment="Stretch" >
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="*" />
			<ColumnDefinition Width="Auto" />
		</Grid.ColumnDefinitions>
		<Grid.RowDefinitions>
			<RowDefinition Height="*" />
		</Grid.RowDefinitions>
		<Grid.Resources>
			<Style x:Key="OpenBrowseButton" TargetType="{x:Type Button}">
				<Setter Property="Content" Value="..." />
				<Setter Property="VerticalContentAlignment" Value="Center"/>
				<Setter Property="HorizontalContentAlignment" Value="Center"/>
				<Setter Property="Padding" Value="10,-4,10,0" />
			</Style>
			<Style TargetType="{x:Type TextBlock}">
				<Setter Property="Padding" Value="2,0" />
				<Setter Property="TextAlignment" Value="Left" />
				<Setter Property="HorizontalAlignment" Value="Left" />
				<Setter Property="VerticalAlignment" Value="Center" />
			</Style>
		</Grid.Resources>
		<TextBox x:Name="FilePathDisplay" Grid.Column="0" behavior:TextBoxEnterKeyBehavior.EnterLosesFocus="True" Text="{Binding Path=FileLocationText, ElementName=FileBrowser, Mode=TwoWay}" Background="{Binding Path=FileValidation, ElementName=FileBrowser, Converter={StaticResource FileValidationColorConverter}}" Width="Auto" MinHeight="20" Height="20" BorderThickness="1" IsReadOnly="False" />
		<Button x:Name="FileBrowseButton" Grid.Column="1" Style="{StaticResource OpenBrowseButton}" BorderThickness="0,1,1,1" Margin="-1,0,0,0" ToolTip="Browse..." BorderBrush="#FFABADB3" FontSize="16" TextOptions.TextFormattingMode="Display" FontFamily="Bitter" Click="FileBrowseButton_Click"/>
	</Grid>
</UserControl>
