<UserControl x:Class="LL.DOS2.SourceControl.Controls.FileBrowseControl"
			 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
			 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
			 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
			 xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
			 xmlns:local="clr-namespace:LL.DOS2.SourceControl.Controls"
			 mc:Ignorable="d" 
			 d:DesignHeight="300" d:DesignWidth="300"
			 x:Name="FileBrowser">
	<Grid MaxHeight="20">
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="*" />
			<ColumnDefinition Width="Auto" />
		</Grid.ColumnDefinitions>
		<Grid.RowDefinitions>
			<RowDefinition Height="*" />
		</Grid.RowDefinitions>
		<Grid.Resources>
			<Style x:Key="OpenBrowseButton" TargetType="{x:Type Button}">
				<Setter Property="Content" Value="..." />
				<Setter Property="HorizontalAlignment" Value="Left"/>
				<Setter Property="Padding" Value="10, 0, 10, 0" />
				<Setter Property="TextBlock.VerticalAlignment" Value="Center" />
				<Setter Property="TextBlock.TextAlignment" Value="Center" />
			</Style>
			<Style TargetType="{x:Type TextBlock}">
				<Setter Property="Padding" Value="2,0" />
				<Setter Property="TextAlignment" Value="Left" />
				<Setter Property="HorizontalAlignment" Value="Left" />
				<Setter Property="VerticalAlignment" Value="Center" />
			</Style>
		</Grid.Resources>
		<TextBox Grid.Column="0" Text="{Binding FileLocationText}" Width="270" MinHeight="20" Height="20" BorderThickness="1" />
		<Button Grid.Column="1" Style="{StaticResource OpenBrowseButton}" Height="20" BorderThickness="0,1,1,1" Margin="-1,0,0,0" ToolTip="Browse..."/>
	</Grid>
</UserControl>
